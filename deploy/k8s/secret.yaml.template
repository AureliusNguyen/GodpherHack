# Template -- do NOT commit a populated copy. Apply via:
#   kubectl create secret generic godpherhack-hub-secrets \
#     -n godpherhack \
#     --from-literal=PINECONE_API_KEY=... \
#     --from-literal=JWT_SECRET=... \
#     --from-literal=GITHUB_CLIENT_ID=... \
#     --from-literal=GITHUB_CLIENT_SECRET=... \
#     --from-literal=ANTHROPIC_API_KEY=...
#
# Or seal with sealed-secrets / external-secrets in a real cluster.
apiVersion: v1
kind: Secret
metadata:
  name: godpherhack-hub-secrets
  namespace: godpherhack
type: Opaque
stringData:
  PINECONE_API_KEY: "REPLACE_ME"
  JWT_SECRET: "REPLACE_ME_AT_LEAST_32_CHARS"
  GITHUB_CLIENT_ID: "REPLACE_ME"
  GITHUB_CLIENT_SECRET: "REPLACE_ME"
  ANTHROPIC_API_KEY: "REPLACE_ME"
